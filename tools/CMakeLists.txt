# tools/ executables (opt-in via LCU_BUILD_TOOLS - see root CMakeLists.txt).
# Only subdirectories with real content are added - see PROJECT_STATE.md/
# TASK_QUEUE.md for what's implemented vs. still an empty directory
# waiting for its phase (asset/debug/mod/server/world tooling: no phase
# has needed them yet).

add_subdirectory(benchmark)
